import { ProductAndQuantity } from './product-and-quantity';

describe('ProductAndQuantity', () => {
  it('should create an instance', () => {
    expect(new ProductAndQuantity()).toBeTruthy();
  });
});
